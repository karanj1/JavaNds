package MyPack.calculate;
import MyPack.*;

public class CalculateInterest extends MyAccount {
	
	public void dispReport()
	{
		double amount = bal + (bal * 0.07 * time);
		
		System.out.println("amount after interest for " + time + " years is : " + amount);
	}
	
}
